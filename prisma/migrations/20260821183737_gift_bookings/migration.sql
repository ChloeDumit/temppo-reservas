-- A class given on the house (birthdays, goodwill). Kept separate from ADMIN so
-- reports can tell a gifted class from one an owner booked against a real pack.
ALTER TYPE "BookingSource" ADD VALUE 'GIFT';
